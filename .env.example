# Backend API — MUST end with /exec (auto-appended if you forget)
#
# Local dev:
#   VITE_API_URL=http://localhost:4000/exec
#
# Vercel / steamoji.online:
#   App auto-uses /exec on HTTPS (proxied to GCP via api/exec.js).
#   Set BACKEND_URL in Vercel env (no VITE_ prefix):
#     BACKEND_URL=http://35.233.224.158:4000
#
VITE_API_URL=http://localhost:4000/exec

VITE_ADMIN_PASS=8031
VITE_ATTEND_PASS=8031
