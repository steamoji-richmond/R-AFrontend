# Local dev only:
VITE_API_URL=http://localhost:4000/exec

# Vercel production — DO NOT set VITE_API_URL to https://steamoji.online/exec
# The app auto-uses /exec on any live host.
#
# Server-side proxy (Vercel env, no VITE_ prefix):
# BACKEND_URL=http://35.233.224.158:4000
#
# Admin + attendance passwords: backend .env only (ADMIN_PASS, ATTEND_PASS).
# Do NOT put passwords in Vite env vars — they would be visible in the JS bundle.
